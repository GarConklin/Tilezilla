Solver enumeration logs, stream dirs, audit JSON, and other run-only output go here. Canonical solve files stay in solves/ per level solvesFile.

Kept long-term: levels-solution-counts.csv (+ README / .gitkeep).
Ingest/sync proof files (ingest-report-*, catalog-sync-*) belong in unused_old/ after a batch is done.

While enumerate scripts run:
  .\scripts\show-solve-runs-status.ps1 -Watch
